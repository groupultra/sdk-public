[
    {
        "implementation": "json",
        "name": "features",
        "load": true,
        "clear": false,

        "settings": {
            "root_dir": "json_db"
        }
    },

    {
        "implementation": "json",
        "load": true,
        "clear": true,
        "name": "real_characters",

        "settings": {
            "root_dir": "json_db"
        }
    },

    {
        "implementation": "json",
        "load": true,
        "clear": false,
        "name": "virtual_characters",

        "settings": {
            "root_dir": "json_db"
        }
    },

    {
        "implementation": "json",
        "load": true,
        "clear": false,
        "name": "states",

        "settings": {
            "root_dir": "json_db"
        }
    },

    {
        "implementation": "json",
        "load": true,
        "clear": false,
        "name": "image_paths",

        "settings": {
            "root_dir": "json_db"
        }
    },

    {
        "implementation": "redis",
        "load": true,
        "clear": false,
        "name": "currency",

        "settings": {
            "root_dir": "json_db",
            "host":"localhost",
            "port":8984
        }
    }
]