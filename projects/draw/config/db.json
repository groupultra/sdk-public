[
    {
        "db_type": "json",
        "name": "real_characters",
        "load": true,
        "clear": true,
        
        "db_config": {
            "root_dir": "draw_json_db"
        }
    },

    {
        "db_type": "json",
        "name": "virtual_characters",
        "load": true,
        "clear": false,

        "db_config": {
            "root_dir": "draw_json_db"
        }
    },

    {
        "db_type": "json",
        "name": "status",
        "load": true,
        "clear": false,

        "db_config": {
            "root_dir": "draw_json_db"
        }
    },

    {
        "db_type": "json",
        "name": "current",
        "load": true,
        "clear": false,

        "db_config": {
            "root_dir": "draw_json_db"
        }
    },

    {
        "db_type": "json",
        "name": "avatars",
        "load": true,
        "clear": false,
        
        "db_config": {
            "root_dir": "draw_json_db"
        }
    }
]