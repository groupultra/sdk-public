[
    {
        "implementation": "json",
        "name": "real_characters",
        "load": true,
        "clear": true,
        
        "settings": {
            "root_dir": "json_db"
        }
    },

    {
        "implementation": "json",
        "name": "virtual_characters",
        "load": true,
        "clear": false,

        "settings": {
            "root_dir": "json_db"
        }
    },

    {
        "implementation": "json",
        "name": "status",
        "load": true,
        "clear": false,

        "settings": {
            "root_dir": "json_db"
        }
    },

    {
        "implementation": "json",
        "name": "current",
        "load": true,
        "clear": false,

        "settings": {
            "root_dir": "json_db"
        }
    },

    {
        "implementation": "json",
        "name": "avatars",
        "load": true,
        "clear": false,
        
        "settings": {
            "root_dir": "json_db"
        }
    }
]